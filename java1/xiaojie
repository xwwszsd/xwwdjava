1.封装，什么是封装？
      把字段(变量，属性)设置成私有，然后对字段进行赋值，传值。提高访问代码安全性。
2.this,JAVA中表示什么？
    当前对象，谁调用它this就代表谁。不能在静态方法中使用。
3.实例变量，静态变量，实例方法，静态方法的访问规则?
    实例变量使用之前需要实例化对象（new）。静态变量可以直接调用。
    实例方法是： 对象名.实例方法名。静态方法是：类名.静态方法名。
4.构造函数的定义，构造函数的作用，和创建对象有什么关联?
    定义：与类名相同，没有返回值，可以有修饰符。作用：主要用来初始化对象。关联：创建对象时如果直接隐式调用构造函数。
什么是构造函数的重载?重载的定义?
    构造函数的重载：构造函数的方法名一样，参数的个数或者数据类型不一样的构造函数。重载：同一个方法名，不同的表现形式（输出结果）。
5.JS的数据类型
  1.数值型 number
  2.字符串类型 string
  3.布尔类型 boolean
  4.未定义类型 undefined
  5.函数类型 function
  6.对象类型 object
  
6.JS方法的定义和参数
  JS定义方法  var x=function(a,b){return a*b}   function fn(a,b){return a*b}   var x=function(a,b){return a*b},var z=x(4,3)
   
   
 构造函数:
  (public) 类名(){
  
  }
  static 可以修饰变量、方法、内部类。被static修饰的是属于类的。
  
  ----------------------
    static {
    	 System.out.println("这是一个静态代码块，只执行一次,不用在入口函数中调用，最先执行");
     }
     {
    	 System.out.println("这是构造(匿名)代码块，创建（实例化）一次对象执行一次");
     }
     public void one() {
    	 System.out.println("这个是(void)实例方法，不能return");
    	 
     }
     
     public static void two() {
    	 System.out.println("这是静态方法，要用：类型.方法名  进行调用");
     }
     
     Userday(){
    	 System.out.println("这是构造函数，创建对象时直接隐式执行。");
     }
     {
    	 System.out.println("这是构造代码块，创建（实例化）一次对象执行一次,按上下顺序执行。");
     }
     
  